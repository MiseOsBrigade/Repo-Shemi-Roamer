# LinkForge Web

A dependency-free progressive web interface for the LinkForge API. It provides
source capture, research-depth selection, job polling, artifact links, local
history, dark mode, keyboard focus, and reduced-motion support.

```bash
python -m http.server 8080
```

Set the API origin and bearer key in the Settings panel. Keys stay in browser
session storage, not persistent local storage.
