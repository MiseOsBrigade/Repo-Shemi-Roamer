# LinkForge Render

Creates branded, accessible HTML and visual PDF reports from a LinkForge
`report.json`, optionally enriched with `evidence.json` and source images.

```bash
python -m pip install -e '.[dev]'
linkforge-render render ../linkforge-core/artifacts/demo/report.json --output artifacts/demo
```
